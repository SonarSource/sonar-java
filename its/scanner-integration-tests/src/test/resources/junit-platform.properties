# Enable JUnit 5 parallel execution. Subclasses of ScannerIntegrationAbstractTest
# are annotated with @Execution(CONCURRENT); these properties activate it.
junit.jupiter.execution.parallel.enabled=true
# Run top-level test classes concurrently with each other (default is same_thread).
junit.jupiter.execution.parallel.mode.classes.default=concurrent
