package org.sonar.java.resolve.targets.se.mb;

public class FakeCommonsLang2ArrayUtils {

  static boolean isEmpty(boolean[] p) {
    return p == null || p.length == 0;
  }

  static boolean isEmpty(byte[] p) {
    return p == null || p.length == 0;
  }

  static boolean isEmpty(char[] p) {
    return p == null || p.length == 0;
  }

  static boolean isEmpty(double[] p) {
    return p == null || p.length == 0;
  }

  static boolean isEmpty(float[] p) {
    return p == null || p.length == 0;
  }

  static boolean isEmpty(int[] p) {
    return p == null || p.length == 0;
  }

  static boolean isEmpty(long[] p) {
    return p == null || p.length == 0;
  }

  static boolean isEmpty(short[] p) {
    return p == null || p.length == 0;
  }

  static boolean isEmpty(Object[] p) {
    return p == null || p.length == 0;
  }

  static boolean isNotEmpty(boolean[] p) {
    return (p != null && p.length != 0);
  }

  static boolean isNotEmpty(byte[] p) {
    return (p != null && p.length != 0);
  }

  static boolean isNotEmpty(char[] p) {
    return (p != null && p.length != 0);
  }

  static boolean isNotEmpty(double[] p) {
    return (p != null && p.length != 0);
  }

  static boolean isNotEmpty(float[] p) {
    return (p != null && p.length != 0);
  }

  static boolean isNotEmpty(int[] p) {
    return (p != null && p.length != 0);
  }

  static boolean isNotEmpty(long[] p) {
    return (p != null && p.length != 0);
  }

  static boolean isNotEmpty(short[] p) {
    return (p != null && p.length != 0);
  }

  static boolean isNotEmpty(Object[] p) {
    return (p != null && p.length != 0);
  }


}
